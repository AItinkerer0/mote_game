# mote

검은 공간에서 빛 물방울이 모든 걸 흡수해 **무한히 성장**하는 미니멀 아케이드 게임.
의존성 없는 단일 `index.html` (vanilla JS + Canvas).

> "컨셉 없는 게 컨셉" — 순백의 몽글한 빛 물방울, 순수한 검은 배경. 헝그리샤크식 무한 성장 + 무한 줌아웃 트릭.

## Status

- **Last reviewed:** 2026-07-17
- **Maturity:** **v1 공개 스냅샷**. 커밋 메시지상 “mote v1 — 미니멀 무한성장 흡수 게임 초기 공개” 이후 카메라 줌 튜닝이 있다. 완성된 상용 타이틀이 아니라, 브라우저에서 바로 열 수 있는 단일 파일 프로토타입/미니 게임이다.
- **검증하지 않은 것:** 이 문서 작성 중 빌드·플레이 테스트·스크린샷 촬영은 하지 않았다. 스크린샷·외부 플레이 영상 링크는 제공하지 않는다.

## 플레이 · 조작

- **이동**: 포인터(마우스/터치)로 물방울을 끌고 다닌다. 방향키도 입력으로 받는다.
- **성장**: 작은 먹이와 나보다 작은 세포를 흡수해 커진다. 커질수록 화면이 줌아웃되며 더 큰 것들이 먹이가 된다.
- **위험**: 나보다 큰 면역세포는 쫓아오고 부딪히면 손해 — 피하거나 더 커져서 삼켜라.
- **티어**: 일정 크기마다 세계의 팔레트(색)가 바뀐다.
- **환생 (prestige)**: `Enter` 또는 화면 버튼 — 지금 크기를 코인으로 정산하고 다시 작게 시작.
- **상점·도감**: 코인으로 gacha를 돌려 주인공 색/모양, 먹이, 배경 **스킨**을 모으고 장착(도감에서 확인). 가차 비용은 코드상 `GACHA_COST = 10`.
- **기타**: `M` — BGM 토글, 게임 오버 후 `R` — 재시작.

## 특징

- 무한 줌아웃 성장 — 수치는 누적되지만 화면상 크기는 줌으로 관리.
- 물방울 물성·흡수 연출, 발광 글로우 비주얼.
- 스킨 시스템: 주인공 색·모양, 먹이 색·모양, 배경(성운·심해·오로라·입자장 등). 배경 미리보기는 실제 플레이 화면을 축소해 보여 주도록 구현되어 있다.
- 효과음·BGM은 **Web Audio API 절차 합성** (외부 오디오 파일 없음).

## 실행

로컬에서 정적 파일을 여는 **미실행 안내**이다.

```bash
# 브라우저로 index.html 직접 열기
# 또는 저장소 루트에서:
python3 -m http.server 8000
# → http://localhost:8000
```

GitHub Pages 배포가 설정되어 있으면 `https://aitinkerer0.github.io/mote_game/` 로 서빙될 수 있다. 이 문서 작성 중 플레이 가능 여부는 브라우저로 검증하지 않았다.

## 저장 · 네트워크 · 라이선스

| 구분 | 내용 |
|------|------|
| 네트워크 | `fetch`/XHR/WebSocket 없음. 외부 CDN·에셋 URL 없음. 오프라인 가능 |
| 로컬 저장 | `localStorage` 키 `virusgame_save` — 코인, 보유/장착 스킨(주인공·먹이·배경) |
| 오디오 | 사용자 제스처 후 Web AudioContext 활성화 (브라우저 정책) |
| 라이선스 | 루트에 license 파일 없음 → **현재 라이선스 미지정** (이 README가 라이선스를 부여하지 않음) |

## 기술

- 단일 HTML 파일, 외부 라이브러리·빌드 단계 없음.
- Canvas 2D, Web Audio API.

---

v1
